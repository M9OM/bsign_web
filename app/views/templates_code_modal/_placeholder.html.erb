<div class="alert">
  <%= svg_icon('info_circle', class: 'w-6 h-6') %>
  <div>
    <p class="font-bold">
      <%= t('api_and_embedding') %>
    </p>
    <p class="text-gray-700">
      <%= t('unlock_with_docuseal_pro') %>
      <br>
      <a class="link font-medium" target="_blank" href="<%= bsign.multitenant? ? console_redirect_index_path(redir: "#{bsign::CONSOLE_URL}/plans") : "#{bsign::CLOUD_URL}/sign_up?#{{ redir: "#{bsign::CONSOLE_URL}/on_premises" }.to_query}" %>" data-turbo="false">
        <%= t('learn_more') %>
      </a>
    </p>
  </div>
</div>
